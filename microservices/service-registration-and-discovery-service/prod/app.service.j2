[Unit]
Description={{ app_name }}
After=syslog.target

[Service]
User={{ username }}
ExecStart="{{ springboot_deploy_folder }}/{{ app_name }}.jar"
SuccessExitStatus=143

[Install]
WantedBy=multi-user.target